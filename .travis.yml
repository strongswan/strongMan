sudo: required

services:
  - docker

language: python
python:
  - "3.4"
  - "3.5"
install:
  - docker build -t strongman docker/

script:
  - docker run --privileged strongman /bin/sh -c "cd /StrongMan/; ipsec start; python manage.py test --settings=strongMan.settings.local"




